# File overview: .env.example
# Purpose: sample environment variables for local and production compose runs.
# Optional overrides for local and production compose runs
# Node API settings
PORT=8000
PYTHON_SERVICE_URL=http://python:5000
MONGODB_URI=mongodb://mongo:27017/solaris
CORS_ORIGINS=http://43.205.98.214,https://43.205.98.214

# Frontend development proxy target (used by Vite in docker-compose dev)
VITE_API_PROXY_TARGET=http://node:8000
